# STRANGERS — FULL MASTER WEBSITE BUILD PROMPT

You are an expert senior frontend engineer, creative developer, UI/UX designer, motion designer, and interactive web developer.

Build the complete **STRANGERS** website as a polished production-quality interactive experience.

Do not create a generic NFT landing page.

Do not create a generic SaaS website.

Do not simplify the concept.

The website must feel like a **living dark interactive world where Strangers characters are actually walking through the environment**, while the user completes a connected waitlist and social-task experience.

Follow every requirement below strictly.

---

# 01 — FIRST: INSPECT THE PROJECT AND ASSETS

Before writing or changing code, inspect the entire project.

Inspect:

* existing source files
* existing components
* existing routes
* existing CSS
* existing Tailwind configuration
* existing assets
* all PNG/JPG/WebP images
* all Strangers character images
* all possible animation frames
* existing social-task configuration
* existing waitlist implementation

Do NOT blindly overwrite the existing project.

Reuse useful existing components/assets when appropriate.

However, if the existing implementation conflicts with this specification, replace the incorrect implementation.

Do not create duplicate waitlist systems.

Do not create duplicate task systems.

Do not create multiple conflicting UI systems.

Create one coherent architecture.

---

# 02 — CORE EXPERIENCE

The entire website revolves around one experience:

```text
ENTER STRANGERS
        ↓
SEE THE LIVING WORLD
        ↓
ENTER EVM WALLET ADDRESS
        ↓
JOIN WAITLIST
        ↓
COMPLETE SOCIAL TASKS
        ↓
FOLLOW X
        ↓
LIKE POST
        ↓
RETWEET POST
        ↓
ALL TASKS COMPLETE
        ↓
YOU'RE IN
```

This must feel like ONE continuous flow.

The waitlist and social tasks MUST NOT feel like two unrelated website sections.

The social tasks are part of the waitlist onboarding process.

---

# 03 — VISUAL DIRECTION

The visual identity must be:

* dark
* mysterious
* minimal
* cinematic
* playful
* premium
* interactive
* atmospheric
* slightly experimental
* NFT / digital collectible inspired
* pixel-art / character-world focused

The website should feel like a small animated world rather than a normal webpage.

The characters are the hero.

The environment is alive.

The UI should support the world instead of overpowering it.

---

# 04 — STRICT COLOR SYSTEM

Use ONLY these two colors for the website UI/environment:

PRIMARY:

#FC6224

BASE:

#1C1C1C

These are the ONLY UI colors.

Do NOT introduce:

* blue
* purple
* green
* yellow
* pink
* cyan
* red
* random gray UI
* white UI
* colorful gradients

Do not use multicolor gradients.

Do not use glow colors outside the specified palette.

Use #FC6224 for:

* buttons
* links
* borders
* active states
* hover states
* focus states
* task completion
* progress indicators
* important UI text
* navigation interactions
* interactive highlights

Use #1C1C1C as the environmental base.

IMPORTANT:

The provided Strangers character artwork is image content.

If the artwork contains its own original colors, PRESERVE those original artwork colors.

Do NOT recolor the provided character artwork simply to force it into the two-color UI system.

The two-color restriction applies to the website UI and environment.

---

# 05 — ABSOLUTELY NO FLAT BACKGROUND

This is a critical requirement.

Do NOT make the background:

```text
solid #1C1C1C
```

and then place a character on top.

That is NOT acceptable.

#1C1C1C is only the BASE.

Build a complete layered animated environment on top of the base.

The background should contain:

* atmosphere
* texture
* depth
* parallax
* moving shapes
* environmental silhouettes
* particles
* foreground elements
* subtle camera movement
* animated shadows
* subtle noise/grain
* distant objects
* midground objects

The user must immediately notice that the environment is moving.

The background should feel alive even when the character is not moving.

---

# 06 — LIVING WORLD ARCHITECTURE

Build the environment as multiple layers.

Use this structure:

```text
┌─────────────────────────────┐
│      ATMOSPHERE / GRAIN     │
├─────────────────────────────┤
│       FAR BACKGROUND        │
├─────────────────────────────┤
│         MIDGROUND           │
├─────────────────────────────┤
│      STRANGER CHARACTERS    │
├─────────────────────────────┤
│         FOREGROUND          │
├─────────────────────────────┤
│          PARTICLES          │
├─────────────────────────────┤
│            UI               │
└─────────────────────────────┘
```

Every layer should have independent movement.

Do not animate everything at the same speed.

---

# 07 — PARALLAX SYSTEM

Implement real multi-layer parallax.

Example:

```text
FAR BACKGROUND
0.10x movement

MIDGROUND
0.30x movement

CHARACTER
1.00x movement

FOREGROUND
1.30x movement
```

Use subtle camera movement based on:

* mouse movement
* pointer position
* viewport movement
* character movement

On mobile, replace mouse interaction with subtle automatic movement.

The effect must create depth without becoming distracting.

---

# 08 — ATMOSPHERIC LAYER

Create a continuously moving atmospheric layer.

Include subtle:

* haze
* noise
* grain
* drifting texture
* environmental shadows
* depth fog
* tonal movement

Do NOT use bright effects.

Keep everything dark and subtle.

The environment should feel rich even though the base is #1C1C1C.

---

# 09 — MOVING ENVIRONMENT

Create abstract environmental objects/silhouettes that move slowly.

Examples:

* distant structures
* dark silhouettes
* abstract shapes
* floating objects
* environmental blocks
* distant scenery
* moving shadows
* subtle architectural forms

These should move independently from the character.

Do NOT make the background look like a particle wallpaper.

It must feel like an actual environment.

---

# 10 — PARTICLE SYSTEM

Add a subtle particle system.

Particles should:

* drift slowly
* have different depths
* have different speeds
* move independently
* respond subtly to parallax
* create atmosphere

Do not make particles bright white.

Keep the environment visually consistent with the dark-charcoal + orange aesthetic.

---

# 11 — STRANGERS CHARACTER ASSETS

Use the ACTUAL Strangers character assets provided in the project.

This is mandatory.

Do NOT:

* generate replacement characters
* create placeholder characters
* use generic NFT characters
* use CSS characters
* use emojis
* use random online images
* redraw the characters

Inspect every provided character asset.

Determine whether the assets are:

* individual characters
* animation frames
* sprite sheets
* walking poses
* different character variations

Build the animation system around the actual assets.

---

# 12 — SPRITE / FRAME ANIMATION — MANDATORY

The character animation MUST use actual sprite/frame animation.

This is NOT optional.

If the project contains individual animation frames:

```text
frame-01.png
frame-02.png
frame-03.png
frame-04.png
frame-05.png
```

create a frame-sequencing system.

Playback:

```text
01 → 02 → 03 → 04 → 05 → 04 → 03 → 02 → loop
```

Use the appropriate frame order based on the actual assets.

If a sprite sheet exists, use sprite-sheet animation.

Use CSS `steps()` where appropriate.

If individual images are required, use efficient frame preloading and frame switching.

Do NOT simply crossfade between character images.

The viewer must perceive a walking animation.

---

# 13 — WALKING MOVEMENT

The character must physically walk through the environment.

The character should:

* walk horizontally
* animate its legs using sprite/frame animation
* have subtle body bounce
* have natural walking timing
* move at different speeds
* enter the scene
* cross the scene
* exit the scene
* reappear later
* occasionally pause
* move at different depths

Combine:

```text
SPRITE FRAME ANIMATION
+
HORIZONTAL MOVEMENT
+
BODY BOUNCE
+
PARALLAX
+
ENVIRONMENT MOVEMENT
```

The final result should look like a character actually walking inside a world.

NOT:

```text
static PNG
+
CSS translateX()
```

The walking frames themselves must animate.

---

# 14 — MULTIPLE STRANGERS

If multiple Stranger assets exist, use them.

Create variation:

* different characters
* different spawn positions
* different walking speeds
* different depths
* different timing
* different directions when appropriate

Do not spawn every character simultaneously.

Characters should appear naturally.

The environment should feel populated without becoming chaotic.

---

# 15 — CHARACTER DEPTH

Create at least three possible character depth levels:

```text
BACKGROUND CHARACTER
smaller + slower

MAIN CHARACTER
normal size + normal speed

FOREGROUND CHARACTER
slightly larger + faster
```

Use scale and movement speed to communicate depth.

The character artwork itself must remain crisp.

Do not blur or distort the artwork.

---

# 16 — MAIN UI

The main UI should be minimal.

Do not cover the entire screen.

The animated world must remain visible.

The main interaction should be a focused floating/embedded experience within the world.

The UI should look like it belongs to Strangers.

Use:

* dark charcoal surfaces
* orange #FC6224 accents
* thin borders
* minimal typography
* subtle animation
* subtle transparency where appropriate
* clean spacing

Avoid:

* generic SaaS cards
* giant white containers
* excessive rounded cards
* generic dashboards
* excessive text
* unnecessary sections

---

# 17 — HERO SCREEN

The initial screen should immediately communicate the Strangers identity.

Show:

* animated Strangers characters
* living environment
* atmospheric background
* subtle parallax
* minimal branding
* primary interaction

The user must see movement immediately.

Do NOT make the first screen a static hero with a giant heading and static image.

The world itself is the hero.

---

# 18 — WAITLIST FORM

The waitlist MUST use an EVM wallet address.

NOT EMAIL.

There must be NO email input.

There must be NO email signup.

There must be NO name field.

There must be NO username field.

The main input:

```text
EVM WALLET ADDRESS
```

Placeholder:

```text
0x...
```

Validate Ethereum/EVM format:

```text
0x + 40 hexadecimal characters
```

Example:

```text
0x1234567890abcdef1234567890abcdef12345678
```

Invalid addresses should show an error state.

Valid addresses should enable the CTA.

Primary button:

```text
JOIN WAITLIST
```

Use #FC6224.

---

# 19 — WALLET ADDRESS UX

Make the address input feel premium.

Include:

* focus animation
* validation
* success state
* invalid state
* paste support
* clear error messaging

If wallet connection functionality already exists in the project, support it.

If wallet connection does not exist, DO NOT pretend that the user is connected.

Allow manual EVM address input.

The user must always understand what address they submitted.

---

# 20 — WAITLIST SUBMISSION

When the user clicks:

**JOIN WAITLIST**

do not immediately navigate to another unrelated page.

Instead transition the SAME interface into the task experience.

Use an animation such as:

```text
WAITLIST
   ↓
success transition
   ↓
TASKS
```

The environment remains visible during the transition.

---

# 21 — SOCIAL TASK SYSTEM

There are exactly THREE core tasks:

### TASK 01

FOLLOW X

CTA:

FOLLOW

### TASK 02

LIKE POST

CTA:

LIKE

### TASK 03

RETWEET POST

CTA:

RETWEET

Do not separate these into a completely different page.

They are part of the waitlist onboarding.

---

# 22 — TASK FLOW

The task flow should feel sequential.

Example:

```text
01 FOLLOW X
      ↓
02 LIKE POST
      ↓
03 RETWEET
```

After completing one task:

* animate completion
* update progress
* unlock the next task
* maintain the same visual environment

Do not make all three tasks feel like unrelated cards.

---

# 23 — TASK DESIGN

Each task should contain:

* task number
* title
* short description
* action
* completion indicator
* progress state

Example:

```text
01

FOLLOW X

Follow Strangers on X

[ FOLLOW ]
```

After completion:

```text
01

FOLLOW X

COMPLETED ✓
```

Use #FC6224 for completion.

---

# 24 — X LINKS

Use the actual X account and post URLs from the existing project if available.

Do NOT invent URLs.

If URLs are not currently configured, create a centralized configuration file:

```text
src/config/socialTasks.ts
```

Example structure:

```text
FOLLOW_URL
LIKE_URL
RETWEET_URL
```

This allows the URLs to be changed easily.

Do not hardcode the same URLs across multiple components.

---

# 25 — TASK COMPLETION

The frontend should clearly show task completion.

Do not falsely claim that an external X action was verified if the application cannot actually verify it.

If external verification is unavailable:

* open the appropriate X action
* allow the user to return
* provide a clear completion interaction
* visually mark the task as completed only according to the implemented verification logic

Do not pretend that a click automatically proves a Follow, Like, or Retweet happened.

---

# 26 — PROGRESS SYSTEM

Display subtle progress.

Example:

```text
1 / 3
```

or:

```text
● ○ ○
```

After each task:

```text
● ● ○
```

After all:

```text
● ● ●
```

Use #FC6224.

The progress should animate smoothly.

---

# 27 — FINAL COMPLETION STATE

After:

* valid EVM address submitted
* Follow completed
* Like completed
* Retweet completed

show:

```text
YOU'RE IN
```

and display the submitted address in shortened form:

```text
0x71...A42F
```

Add a subtle completion animation.

Do not ask for email.

Do not ask for additional information.

---

# 28 — MICRO INTERACTIONS

Every interactive element should have subtle motion.

Buttons:

* hover
* press
* focus
* completion

Inputs:

* focus
* validation
* success
* error

Tasks:

* unlock animation
* completion animation
* progress animation

Navigation:

* subtle hover response

Use smooth transitions.

Do not use excessive bouncing.

Do not use generic template animations.

---

# 29 — TYPOGRAPHY

Typography should be:

* bold
* modern
* minimal
* readable
* slightly experimental

Use strong hierarchy.

Do not overload the screen with text.

The character/environment should remain the main visual focus.

---

# 30 — RESPONSIVE DESIGN

The website must work on:

* desktop
* laptop
* tablet
* mobile

Desktop:

* large animated world
* visible parallax
* characters can travel across the scene

Mobile:

* preserve character visibility
* preserve animation
* simplify environment if necessary for performance
* maintain readable task UI
* keep EVM input usable
* keep buttons easy to tap

Do not simply shrink the desktop layout.

Create proper responsive behavior.

---

# 31 — PERFORMANCE

Target smooth animation.

Use GPU-friendly properties:

* transform
* translate3d
* opacity
* scale
* rotation

Avoid unnecessary layout recalculation.

Preload animation frames.

Do not load the same image repeatedly.

Use efficient particle rendering.

If appropriate, use Canvas for background particles/environmental effects.

Do not sacrifice visual quality unnecessarily.

---

# 32 — ACCESSIBILITY

Include:

* keyboard navigation
* visible focus states
* accessible buttons
* accessible input labels
* proper contrast
* reduced-motion support

If the user enables reduced motion, reduce environmental movement while preserving functionality.

---

# 33 — PROJECT ARCHITECTURE

Use:

* React
* TypeScript
* Vite
* Tailwind CSS

Use component-based architecture.

Suggested structure:

```text
src/
│
├── components/
│   ├── world/
│   │   ├── LivingWorld.tsx
│   │   ├── Atmosphere.tsx
│   │   ├── BackgroundLayer.tsx
│   │   ├── MidgroundLayer.tsx
│   │   ├── ForegroundLayer.tsx
│   │   ├── ParticleLayer.tsx
│   │   ├── ParallaxLayer.tsx
│   │   └── Environment.tsx
│   │
│   ├── characters/
│   │   ├── Stranger.tsx
│   │   ├── StrangerSprite.tsx
│   │   ├── SpriteAnimator.tsx
│   │   └── CharacterMovement.tsx
│   │
│   ├── waitlist/
│   │   ├── WaitlistFlow.tsx
│   │   ├── WalletAddressInput.tsx
│   │   ├── WaitlistForm.tsx
│   │   └── WaitlistSuccess.tsx
│   │
│   ├── tasks/
│   │   ├── TaskFlow.tsx
│   │   ├── TaskItem.tsx
│   │   ├── TaskProgress.tsx
│   │   └── TaskCompletion.tsx
│   │
│   └── ui/
│       ├── Button.tsx
│       ├── Input.tsx
│       └── Modal.tsx
│
├── hooks/
│   ├── useSpriteAnimation.ts
│   ├── useCharacterMovement.ts
│   ├── useParallax.ts
│   ├── useReducedMotion.ts
│   └── useTaskProgress.ts
│
├── config/
│   ├── socialTasks.ts
│   └── constants.ts
│
├── assets/
│   └── strangers/
│
├── pages/
│
├── App.tsx
└── main.tsx
```

Do not put the entire application inside App.tsx.

---

# 34 — STATE ARCHITECTURE

The main flow should have clear states:

```text
INTRO
↓
WAITLIST
↓
WAITLIST_SUCCESS
↓
TASK_1
↓
TASK_2
↓
TASK_3
↓
COMPLETED
```

Keep the state centralized.

Do not duplicate state between components.

The submitted wallet address must persist throughout the flow.

---

# 35 — NO EMAIL ANYWHERE

This is a HARD REQUIREMENT.

Search the entire codebase for:

```text
email
e-mail
mail
newsletter
```

Remove email-based waitlist functionality unless it is completely unrelated to this website.

The Strangers waitlist must use:

**EVM WALLET ADDRESS**

---

# 36 — NO SEPARATE TASK SECTION

Search the project for existing social task sections.

If tasks currently appear as an unrelated page/section, restructure them.

The final architecture must be:

```text
WAITLIST
     ↓
TASK FLOW
```

not:

```text
WAITLIST

----------------

SOCIAL TASK SECTION
```

---

# 37 — NO GENERIC HERO

Do NOT create:

```text
STRANGERS

The next generation NFT collection.

[JOIN NOW]

static image
```

This is NOT the desired experience.

The animated world itself must dominate the first screen.

---

# 38 — NO STATIC CHARACTER

A character standing still is a failure.

The first viewport must visibly demonstrate:

**SPRITE/FRAME WALKING ANIMATION**

The user should be able to see the character walking without clicking anything.

---

# 39 — NO FLAT BACKGROUND

A completely flat #1C1C1C background is a failure.

The first viewport must visibly contain:

* atmosphere
* texture
* depth
* parallax
* moving environment
* particles
* animated characters

---

# 40 — FINAL VISUAL TARGET

The final website should feel like:

> A dark, atmospheric, animated 2D world.

> Strangers characters walk through the environment using actual sprite/frame animation.

> The environment continuously moves independently through layered parallax, texture, atmosphere, particles, and subtle environmental elements.

> The user enters an EVM wallet address and joins the waitlist.

> The same experience then transitions naturally into Follow X → Like → Retweet.

> After completion, the user receives a strong "YOU'RE IN" confirmation.

The website should feel like a **living digital world**, not a normal website.

---

# 41 — FINAL QA BEFORE COMPLETION

Do NOT tell me the project is complete until you have tested all of the following.

## CHARACTER

[ ] Actual provided Strangers assets are used.

[ ] No placeholder characters.

[ ] Character uses sprite/frame animation.

[ ] Walking frames visibly change.

[ ] Character physically moves through the environment.

[ ] Walking animation loops correctly.

[ ] Character has subtle body movement.

[ ] Multiple characters use appropriate variation when assets allow.

## WORLD

[ ] Base background is #1C1C1C.

[ ] Background is NOT flat.

[ ] Atmosphere exists.

[ ] Texture/grain exists.

[ ] Far background exists.

[ ] Midground exists.

[ ] Foreground exists.

[ ] Moving shapes/environment exists.

[ ] Particle system exists.

[ ] Parallax exists.

[ ] Different layers move at different speeds.

[ ] World remains visually alive without user interaction.

## COLOR

[ ] UI primary color is #FC6224.

[ ] Environmental base is #1C1C1C.

[ ] No random UI colors.

[ ] No blue UI.

[ ] No purple UI.

[ ] No green UI.

[ ] No yellow UI.

[ ] No unnecessary white UI.

[ ] No unauthorized gradients.

[ ] Character artwork retains original colors.

## WAITLIST

[ ] Waitlist uses EVM wallet address.

[ ] No email field.

[ ] No email signup.

[ ] Address format is validated.

[ ] Invalid address shows an error.

[ ] Valid address can continue.

[ ] Address persists through the flow.

## TASKS

[ ] Follow X exists.

[ ] Like Post exists.

[ ] Retweet exists.

[ ] Tasks are part of the waitlist flow.

[ ] Tasks are NOT an unrelated separate section.

[ ] Progress updates after each task.

[ ] Completed state is visually clear.

[ ] X URLs are centralized.

## FINAL STATE

[ ] All tasks complete.

[ ] Final "YOU'RE IN" state appears.

[ ] Wallet address is displayed in shortened format.

[ ] No email is requested.

## RESPONSIVENESS

[ ] Desktop works.

[ ] Tablet works.

[ ] Mobile works.

[ ] Character remains visible.

[ ] World remains animated.

[ ] Wallet input works on mobile.

[ ] Tasks are usable on mobile.

## PERFORMANCE

[ ] Character frames are preloaded.

[ ] Animations are smooth.

[ ] No obvious frame stuttering.

[ ] No unnecessary repeated image loading.

[ ] Background animation does not freeze the UI.

[ ] Target approximately 60 FPS where hardware allows.

---

# 42 — MOST IMPORTANT RULE

Do not optimize for the fastest implementation.

Optimize for the **correct visual and interactive result**.

Do not replace complex requirements with simpler approximations.

Do not silently remove requirements.

Do not invent a different design.

Do not create a generic website.

Do not use a static character.

Do not use a flat background.

Do not use email.

Do not separate the tasks from the waitlist flow.

The final result must clearly communicate:

**STRANGERS ARE ALIVE.**

